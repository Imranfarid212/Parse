# T1.5 Phase Lock Proof

Temporary proof branch for B1 T1.5 validation.

Expected CI behavior: a PR carrying the `phase:b2` label must be blocked while
`gates/phases.json` keeps `b1.state` below `passed`.
